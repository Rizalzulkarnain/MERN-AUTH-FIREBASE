import React from 'react';

import { Text, FooterContainer } from '../../styles';

const Footer = () => {
  return (
    <FooterContainer>
      <Text mb={2}>Copyright &copy; Authentication</Text>
    </FooterContainer>
  );
};

export default Footer;
